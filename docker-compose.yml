services:
  bot:
    build: .
    env_file: .env
    volumes:
      - db-data:/app/db.json
      - whisper-cache:/root/.cache/huggingface
    restart: unless-stopped

volumes:
  db-data:
  whisper-cache:
